all:
  children:
    mongo:
      hosts:
        mongo1:
          ansible_host: (Replace with Private IP of user1)
        mongo2:
          ansible_host: (Replace with Private IP of user2)
  vars:
    ansible_user: (replace it with your user name)
    ansible_become: true
    ansible_ssh_private_key_file: /home/ubuntu/.ssh/(replace it with your private key name).pem
